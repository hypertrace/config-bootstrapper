attributes.service.config = {
    host=localhost
    port=9012
}
entity.service.config = {
    host=localhost
    port=50061
}

document.store {
  dataStoreType = mongo
  appName = config-bootstrapper-integration-test
  maxPoolSize = 10
  mongo {
    database = default_db
    endpoints = [
      {
        host = localhost
        host = ${?MONGO_HOST}
        port = 37017
        port = ${?MONGO_PORT}
      }
    ]
  }
}